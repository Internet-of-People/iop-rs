// TODO ocnsider if this worths a separate interface or should be merged into e.g. IClaimManager
// TODO how to add a new process? Is it done within this same interface or via a separate one?
//pub trait ContentStorage {
//    getProcess(id: &ProcessId) -> Fallible<Process>;
//}
